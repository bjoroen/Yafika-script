let add = fn(x, y) { x + y }

if(add(1,1) > 3){
  return 5
} else {
  return 10
}

// panicked at src/parser.rs:170:13:
// Syntax error, Token {
//     token_type: RightParen,
//     literal: ")",
// }
